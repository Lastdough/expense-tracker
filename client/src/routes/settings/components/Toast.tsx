// Re-export from the shared components folder; the settings screen was the
// first consumer but Quick-Add, Expenses, etc. also need it.
export { Toast, type ToastState } from '../../../components/Toast';
