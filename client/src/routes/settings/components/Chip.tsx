// Re-export from the shared components folder so existing settings imports
// keep working while new screens (Quick-Add, Expenses list, etc.) import the
// shared path directly.
export { Chip } from '../../../components/Chip';
